<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Make Admin</title>
</head>
<body>
  <h1>Make Admin</h1>
  <ul>
    <% users.forEach(user => { %>
      <li>
        <%= user.first_name %> <%= user.last_name %>
        <form action="/auth/make-admin/<%= user.id %>" method="POST" style="display:inline;">
          <button type="submit">Make Admin</button>
        </form>
      </li>
    <% }) %>
  </ul>
</body>
</html>